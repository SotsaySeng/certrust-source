apiVersion: v1
kind: Secret
metadata:
  name: {{ include "certrust.secretName" . }}
  labels:
    {{- include "certrust.labels" . | nindent 4 }}
type: Opaque
stringData:
  JWT_SECRET: {{ include "certrust.secretValue" (dict "root" $ "explicit" .Values.secrets.jwtSecret "key" "JWT_SECRET" "length" 32) | quote }}
  ADMIN_JWT_SECRET: {{ include "certrust.secretValue" (dict "root" $ "explicit" .Values.secrets.adminJwtSecret "key" "ADMIN_JWT_SECRET" "length" 32) | quote }}
  APP_KEYS: {{ include "certrust.secretValue" (dict "root" $ "explicit" .Values.secrets.appKeys "key" "APP_KEYS" "length" 32) | quote }}
  API_TOKEN_SALT: {{ include "certrust.secretValue" (dict "root" $ "explicit" .Values.secrets.apiTokenSalt "key" "API_TOKEN_SALT" "length" 32) | quote }}
  TRANSFER_TOKEN_SALT: {{ include "certrust.secretValue" (dict "root" $ "explicit" .Values.secrets.transferTokenSalt "key" "TRANSFER_TOKEN_SALT" "length" 32) | quote }}
  ENCRYPTION_KEY: {{ include "certrust.secretValue" (dict "root" $ "explicit" .Values.secrets.encryptionKey "key" "ENCRYPTION_KEY" "length" 32) | quote }}
  {{- if .Values.postgresql.enabled }}
  DATABASE_PASSWORD: {{ include "certrust.secretValue" (dict "root" $ "explicit" .Values.postgresql.password "key" "DATABASE_PASSWORD" "length" 24) | quote }}
  {{- end }}
  {{- if .Values.s3.enabled }}
  # No auto-generation fallback here (unlike the values above) - there's no
  # such thing as a generated-but-valid AWS/S3 credential, so these must
  # come from .Values.s3.accessKeyId/secretAccessKey.
  S3_ACCESS_KEY_ID: {{ .Values.s3.accessKeyId | quote }}
  S3_SECRET_ACCESS_KEY: {{ .Values.s3.secretAccessKey | quote }}
  {{- end }}
