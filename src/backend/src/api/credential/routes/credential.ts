/**
 * credential router
 */

import { factories } from '@strapi/strapi'

// The default router. Since we have custom route files for both
// public and authenticated routes, we can leave this empty.
// Strapi will merge all route files together.
export default {
  routes: []
} 