/**
 * design-template service
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreService('api::design-template.design-template', ({ strapi }) => ({
  // Custom service methods for design-template
}))
