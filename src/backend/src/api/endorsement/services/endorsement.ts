/**
 * endorsement service
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreService('api::endorsement.endorsement', ({ strapi }) => ({
  // Custom service methods for endorsement
})) 