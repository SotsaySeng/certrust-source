/**
 * evidence service
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreService('api::evidence.evidence', ({ strapi }) => ({
  // Custom service methods for evidence
})) 