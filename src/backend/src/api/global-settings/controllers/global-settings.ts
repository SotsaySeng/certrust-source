/**
 * global-settings controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::global-settings.global-settings', ({ strapi }) => ({
  // Custom controller methods for global-settings
}))
