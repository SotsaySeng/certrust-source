/**
 * global-settings service
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreService('api::global-settings.global-settings', ({ strapi }) => ({
  // Custom service methods for global-settings
}))
