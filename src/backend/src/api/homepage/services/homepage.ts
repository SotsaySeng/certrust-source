/**
 * homepage service
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreService('api::homepage.homepage', ({ strapi }) => ({
  // Custom service methods for homepage
}))
