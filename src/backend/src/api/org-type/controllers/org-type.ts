/**
 * org-type controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::org-type.org-type', ({ strapi }) => ({
  // Custom controller methods for org-type
}))
