/**
 * org-type service
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreService('api::org-type.org-type', ({ strapi }) => ({
  // Custom service methods for org-type
}))
