/**
 * profile service
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreService('api::profile.profile', ({ strapi }) => ({
  // Custom service methods for profile
})) 