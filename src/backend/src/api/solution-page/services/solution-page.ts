/**
 * solution-page service
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreService('api::solution-page.solution-page', ({ strapi }) => ({
  // Custom service methods for solution-page
}))
